import Link from "next/link"; import type { Metadata } from "next"; import { notFound } from "next/navigation"; import { publicArticles } from "@/lib/content/public-content"; import { publicMetadata } from "@/lib/seo/public";
export function generateStaticParams() { return publicArticles.map(article => ({ slug: article.slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const article = publicArticles.find(item => item.slug === slug); return article ? publicMetadata(article.title, article.description, `/conteudos/${slug}`) : {}; }
export default async function Page({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const article = publicArticles.find(item => item.slug === slug); if (!article) notFound(); return <main className="min-h-screen bg-slate-50 px-5 py-16 text-slate-950"><article className="mx-auto max-w-3xl"><Link href="/conteudos" className="text-sm text-brand-700">← Todos os conteúdos</Link><p className="mt-8 text-sm font-semibold uppercase tracking-[.18em] text-brand-600">Guia IA Recepção</p><h1 className="mt-4 text-4xl font-semibold tracking-tight sm:text-5xl">{article.title}</h1><p className="mt-5 text-xl leading-8 text-slate-600">{article.description}</p><div className="mt-10 space-y-8">{article.sections.map(section => <section key={section.heading}><h2 className="text-2xl font-semibold">{section.heading}</h2><p className="mt-3 leading-8 text-slate-700">{section.text}</p></section>)}</div><Link href="/demonstracao" className="mt-12 inline-block rounded-control bg-brand-600 px-5 py-3 font-semibold text-white">Conversar sobre minha clínica</Link></article></main>; }
