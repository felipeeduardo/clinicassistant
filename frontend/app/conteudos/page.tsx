import Link from "next/link"; import type { Metadata } from "next"; import { publicArticles } from "@/lib/content/public-content"; import { publicMetadata } from "@/lib/seo/public";
export const metadata: Metadata = publicMetadata("Conteúdos para clínicas", "Guias práticos sobre recepção, WhatsApp, agenda e automação para clínicas.", "/conteudos");
export default function Page() { return <main className="min-h-screen bg-slate-50 px-5 py-16 text-slate-950"><div className="mx-auto max-w-5xl"><Link href="/" className="text-sm text-brand-700">← IA Recepção</Link><h1 className="mt-8 text-4xl font-semibold">Conteúdos para uma recepção mais simples</h1><p className="mt-4 max-w-2xl text-lg text-slate-600">Ideias práticas para organizar atendimento, agenda e WhatsApp.</p><div className="mt-10 grid gap-5 sm:grid-cols-2">{publicArticles.map(article => <Link href={`/conteudos/${article.slug}`} className="rounded-panel border border-slate-200 bg-white p-6 transition hover:-translate-y-0.5 hover:shadow-floating" key={article.slug}><h2 className="text-xl font-semibold">{article.title}</h2><p className="mt-3 text-slate-600">{article.description}</p><span className="mt-5 inline-block text-sm font-semibold text-brand-700">Ler conteúdo →</span></Link>)}</div></div></main>; }
