"use client";

import { useQueries, useQuery } from "@tanstack/react-query";
import { useState } from "react";
import { LoadingRow, RequestFeedback } from "@/components/request-feedback";
import { ProtectedShell } from "@/components/protected-shell";
import { ProfessionalForm } from "@/features/catalog/catalog-forms";
import { catalogApi } from "@/lib/api/catalog";
import { can } from "@/lib/auth/permissions";
import type { Professional, ProfessionalSchedule } from "@/lib/api/types";
import { useApi, useAuth } from "@/providers/providers";

export default function ProfessionalsPage() { const api = useApi(); const { user } = useAuth(); const professionals = useQuery({ queryKey: ["professionals"], queryFn: () => catalogApi.listProfessionals(api) }); const [units, specialties] = useQueries({ queries: [{ queryKey: ["units"], queryFn: () => catalogApi.listUnits(api) }, { queryKey: ["specialties"], queryFn: () => catalogApi.listSpecialties(api) }] }); const [editing, setEditing] = useState<Professional | null | undefined>(); const [schedule, setSchedule] = useState<{ professional: Professional; data: ProfessionalSchedule } | null>(null); const formReady = !!units.data && !!specialties.data; const allowed = can(user, "manageCatalog"); const period = () => ({ startsAt: new Date().toISOString(), endsAt: new Date(Date.now() + 7 * 86400000).toISOString() }); return <ProtectedShell><header className="flex flex-wrap items-center justify-between gap-3"><div><h1 className="text-2xl font-bold">Profissionais</h1><p className="mt-1 text-slate-600">Cadastros do tenant atual.</p></div>{allowed && <button className="rounded bg-blue-700 px-4 py-2 text-white disabled:opacity-50" disabled={!formReady} onClick={() => setEditing(null)}>Novo profissional</button>}</header>{editing !== undefined && formReady && allowed && <ProfessionalForm professional={editing ?? undefined} units={units.data} specialties={specialties.data} onComplete={() => setEditing(undefined)} onCancel={() => setEditing(undefined)} />}<div className="mt-6 overflow-x-auto rounded bg-white shadow"><table className="w-full text-left"><thead><tr className="border-b"><th className="p-3">Nome</th><th className="p-3">E-mail</th><th className="p-3">Registro</th><th className="p-3">Status</th><th className="p-3">Ações</th></tr></thead><tbody>{professionals.isLoading && <LoadingRow columns={5} />}{professionals.data?.map(item => <tr className="border-b" key={item.id}><td className="p-3">{item.name}</td><td className="p-3">{item.email}</td><td className="p-3">{item.registrationNumber}</td><td className="p-3">{item.status}</td><td className="p-3"><button className="mr-3 text-blue-700 underline" onClick={async () => { const range = period(); setSchedule({ professional: item, data: await catalogApi.getProfessionalSchedule(api, item.id, range.startsAt, range.endsAt) }); }}>Agenda</button>{allowed && <button className="text-blue-700 underline" onClick={() => setEditing(item)}>Editar</button>}</td></tr>)}</tbody></table><RequestFeedback error={professionals.error} /></div>{schedule && <section className="mt-5 rounded bg-white p-4 shadow"><div className="flex justify-between"><h2 className="text-lg font-bold">Agenda: {schedule.professional.name}</h2><button className="underline" onClick={() => setSchedule(null)}>Fechar</button></div><p className="mt-2 text-sm">Próximos 7 dias</p><ul className="mt-2 list-disc pl-5 text-sm"><li>{schedule.data.appointments.length} consulta(s)</li><li>{schedule.data.blocks.length} bloqueio(s)</li><li>{schedule.data.vacations.length} período(s) de férias</li></ul></section>}</ProtectedShell>; }
