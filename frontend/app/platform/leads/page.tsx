"use client";

import { useState } from "react";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { ProtectedShell } from "@/components/protected-shell";
import { PageContainer, PageHeader } from "@/components/ui/page";
import { Button } from "@/components/ui/button";
import { Card, StatusBadge } from "@/components/ui/surfaces";
import { useApi, useAuth } from "@/providers/providers";
import { platformApi } from "@/lib/api/platform";

const statuses = ["New", "Contacted", "Qualified", "DemoScheduled", "Won", "Lost", "Archived"];
const labels: Record<string, string> = { New: "Novo", Contacted: "Contatado", Qualified: "Qualificado", DemoScheduled: "Demonstração agendada", Won: "Convertido", Lost: "Perdido", Archived: "Arquivado" };
export default function PlatformLeadsPage() {
  const api = useApi(); const { user } = useAuth(); const client = useQueryClient(); const [status, setStatus] = useState(""); const [search, setSearch] = useState(""); const [selected, setSelected] = useState<string | null>(null); const [note, setNote] = useState("");
  const query = useQuery({ queryKey: ["platform", "leads", status, search], queryFn: () => platformApi.leads(api, new URLSearchParams({ ...(status ? { status } : {}), ...(search ? { search } : {}) }).toString()), enabled: user?.role === "PlatformAdmin" });
  const summary = useQuery({ queryKey: ["platform", "lead-summary"], queryFn: () => platformApi.leadSummary(api), enabled: user?.role === "PlatformAdmin" });
  const detail = useQuery({ queryKey: ["platform", "lead", selected], queryFn: () => platformApi.lead(api, selected!), enabled: Boolean(selected) });
  const updateStatus = useMutation({ mutationFn: (value: string) => platformApi.leadStatus(api, selected!, value), onSuccess: () => { void client.invalidateQueries({ queryKey: ["platform", "leads"] }); void client.invalidateQueries({ queryKey: ["platform", "lead", selected] }); void client.invalidateQueries({ queryKey: ["platform", "lead-summary"] }); } });
  const addNote = useMutation({ mutationFn: () => platformApi.leadNote(api, selected!, note), onSuccess: () => { setNote(""); void client.invalidateQueries({ queryKey: ["platform", "lead", selected] }); } });
  const assign = useMutation({ mutationFn: () => platformApi.leadAssign(api, selected!, user!.id), onSuccess: () => { void client.invalidateQueries({ queryKey: ["platform", "lead", selected] }); } });
  if (user?.role !== "PlatformAdmin") return <ProtectedShell><PageContainer><PageHeader pathname="/platform/leads" title="Acesso negado" description="Esta área é exclusiva da administração da plataforma." /></PageContainer></ProtectedShell>;
  return <ProtectedShell><PageContainer><PageHeader pathname="/platform/leads" title="Leads comerciais" description="Acompanhe solicitações de demonstração sem criar clínicas automaticamente." /><section className="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-4">{[["Novos", summary.data?.new], ["Contatados", summary.data?.contacted], ["Qualificados", summary.data?.qualified], ["Total", summary.data?.total]].map(([label, value]) => <Card className="p-4" key={String(label)}><p className="text-xs text-slate-500">{label}</p><p className="mt-1 text-2xl font-semibold">{value ?? "—"}</p></Card>)}</section><Card className="mt-6 p-4"><div className="flex flex-wrap gap-3"><input aria-label="Buscar lead" className="rounded-control border border-border-system px-3 py-2 text-sm" placeholder="Buscar por nome, empresa ou e-mail" value={search} onChange={event => setSearch(event.target.value)} /><select aria-label="Filtrar status" className="rounded-control border border-border-system px-3 py-2 text-sm" value={status} onChange={event => setStatus(event.target.value)}><option value="">Todos os status</option>{statuses.map(item => <option key={item} value={item}>{labels[item]}</option>)}</select></div></Card><Card className="mt-4 overflow-hidden p-0"><div className="divide-y divide-border-system">{query.data?.items.map(item => <button className="grid w-full gap-2 px-5 py-4 text-left hover:bg-slate-50 sm:grid-cols-[1fr_1fr_auto]" key={item.id} onClick={() => setSelected(item.id)}><span><strong className="block text-sm">{item.fullName}</strong><span className="text-xs text-slate-500">{item.companyOrClinicName}</span></span><span className="text-sm text-slate-600">{item.email}<br />{item.phone}</span><StatusBadge tone={item.status === "New" ? "info" : item.status === "Lost" ? "danger" : "neutral"}>{labels[item.status] ?? item.status}</StatusBadge></button>)}{query.isLoading && <p className="p-6 text-sm text-slate-500">Carregando leads...</p>}{!query.isLoading && !query.data?.items.length && <p className="p-6 text-sm text-slate-500">Nenhum lead encontrado.</p>}</div></Card>{selected && detail.data && <Card className="mt-4 p-5"><div className="flex items-start justify-between gap-4"><div><h2 className="text-xl font-semibold">{detail.data.lead.fullName}</h2><p className="text-sm text-slate-500">{detail.data.lead.companyOrClinicName} · {detail.data.lead.email}</p></div><Button variant="outline" loading={assign.isPending} onClick={() => assign.mutate()}>Assumir lead</Button><Button variant="ghost" onClick={() => setSelected(null)}>Fechar</Button></div><p className="mt-4 whitespace-pre-wrap text-sm text-slate-700">{detail.data.description || "Sem descrição adicional."}</p><div className="mt-5 flex flex-wrap gap-2">{statuses.map(item => <Button key={item} size="sm" variant={item === detail.data?.lead.status ? "primary" : "outline"} loading={updateStatus.isPending && item !== detail.data?.lead.status} onClick={() => updateStatus.mutate(item)}>{labels[item]}</Button>)}</div><div className="mt-5 border-t border-border-system pt-4"><label className="text-sm font-medium" htmlFor="lead-note">Nova observação</label><textarea id="lead-note" className="mt-2 min-h-24 w-full rounded-control border border-border-system p-3 text-sm" value={note} onChange={event => setNote(event.target.value)} /><Button className="mt-3" size="sm" loading={addNote.isPending} disabled={!note.trim()} onClick={() => addNote.mutate()}>Registrar observação</Button></div><ul className="mt-5 space-y-2 text-xs text-slate-500">{detail.data.history.map(item => <li key={`${item.createdAt}-${item.note}`}>{new Date(item.createdAt).toLocaleString("pt-BR")} · {item.actorName ?? "Sistema"} · {item.note}</li>)}</ul></Card>}</PageContainer></ProtectedShell>;
}
