"use client";

import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { useState } from "react";
import { LoadingRow, RequestFeedback } from "@/components/request-feedback";
import { ProtectedShell } from "@/components/protected-shell";
import { SpecialtyForm } from "@/features/catalog/catalog-forms";
import { catalogApi } from "@/lib/api/catalog";
import { can } from "@/lib/auth/permissions";
import type { Specialty, SpecialtyDependencies } from "@/lib/api/types";
import { useApi, useAuth } from "@/providers/providers";

export default function SpecialtiesPage() { const api = useApi(); const { user } = useAuth(); const queryClient = useQueryClient(); const specialties = useQuery({ queryKey: ["specialties"], queryFn: () => catalogApi.listSpecialties(api) }); const [editing, setEditing] = useState<Specialty | null | undefined>(); const [dependencies, setDependencies] = useState<{ specialty: Specialty; data: SpecialtyDependencies } | null>(null); const allowed = can(user, "manageCatalog"); const status = useMutation({ mutationFn: ({ id, next }: { id: string; next: "Active" | "Inactive" }) => catalogApi.setSpecialtyStatus(api, id, next), onSuccess: () => queryClient.invalidateQueries({ queryKey: ["specialties"] }) }); return <ProtectedShell><header className="flex flex-wrap items-center justify-between gap-3"><div><h1 className="text-2xl font-bold">Especialidades</h1><p className="mt-1 text-slate-600">Cadastros do tenant atual.</p></div>{allowed && <button className="rounded bg-blue-700 px-4 py-2 text-white" onClick={() => setEditing(null)}>Nova especialidade</button>}</header>{editing !== undefined && allowed && <SpecialtyForm specialty={editing ?? undefined} onComplete={() => setEditing(undefined)} onCancel={() => setEditing(undefined)} />}<div className="mt-6 overflow-x-auto rounded bg-white shadow"><table className="w-full text-left"><thead><tr className="border-b"><th className="p-3">Nome</th><th className="p-3">Descrição</th><th className="p-3">Status</th><th className="p-3">Ações</th></tr></thead><tbody>{specialties.isLoading && <LoadingRow columns={4} />}{specialties.data?.map(item => <tr className="border-b" key={item.id}><td className="p-3">{item.name}</td><td className="p-3">{item.description || "—"}</td><td className="p-3">{item.status}</td><td className="p-3"><button className="mr-3 text-blue-700 underline" onClick={async () => setDependencies({ specialty: item, data: await catalogApi.getSpecialtyDependencies(api, item.id) })}>Dependências</button>{allowed && <><button className="mr-3 text-blue-700 underline" onClick={() => setEditing(item)}>Editar</button><button className="text-blue-700 underline" onClick={() => status.mutate({ id: item.id, next: item.status === "Active" ? "Inactive" : "Active" })}>{item.status === "Active" ? "Desativar" : "Ativar"}</button></>}</td></tr>)}</tbody></table><RequestFeedback error={specialties.error ?? status.error} /></div>{dependencies && <section className="mt-5 rounded bg-white p-4 shadow"><div className="flex justify-between"><h2 className="text-lg font-bold">Dependências: {dependencies.specialty.name}</h2><button className="underline" onClick={() => setDependencies(null)}>Fechar</button></div><p className="mt-2 text-sm">Profissionais: {dependencies.data.professionals} · Consultas futuras: {dependencies.data.futureAppointments}</p><p className="mt-1 text-sm">{dependencies.data.canDeactivate ? "Pode ser desativada ou excluída com segurança." : "Não pode ser desativada ou excluída enquanto houver dependências."}</p></section>}</ProtectedShell>; }
