import type { ApiClient } from "@/lib/api/client";
import type { OnboardTenantRequest, PlatformClinic, PlatformTenant, PlatformUser } from "@/lib/api/types";
export const platformApi = { tenants: (api: ApiClient) => api.request<PlatformTenant[]>("/api/platform/tenants"), users: (api: ApiClient) => api.request<PlatformUser[]>("/api/platform/users"), clinics: (api: ApiClient) => api.request<PlatformClinic[]>("/api/platform/clinics"), status: (api: ApiClient, id: string, action: "activate" | "suspend" | "disable") => api.request<void>(`/api/platform/tenants/${id}/${action}`, { method: "POST" }), onboard: (api: ApiClient, request: OnboardTenantRequest, idempotencyKey: string) => api.request("/api/platform/onboarding", { method: "POST", headers: { "Idempotency-Key": idempotencyKey }, body: JSON.stringify(request) }) };
