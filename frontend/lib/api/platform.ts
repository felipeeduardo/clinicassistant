import type { ApiClient } from "@/lib/api/client";
import type { DemoLeadDetail, DemoLeadPage, DemoLeadSummary, OnboardTenantRequest, OnboardTenantResponse, PlatformClinic, PlatformOnboardingStatus, PlatformTenant, PlatformUser } from "@/lib/api/types";
export const platformApi = { tenants: (api: ApiClient) => api.request<PlatformTenant[]>("/api/platform/tenants"), users: (api: ApiClient) => api.request<PlatformUser[]>("/api/platform/users"), clinics: (api: ApiClient) => api.request<PlatformClinic[]>("/api/platform/clinics"), onboardingStatus: (api: ApiClient, tenantId: string) => api.request<PlatformOnboardingStatus>(`/api/platform/onboarding/${tenantId}`), status: (api: ApiClient, id: string, action: "activate" | "suspend" | "disable") => api.request<void>(`/api/platform/tenants/${id}/${action}`, { method: "POST" }), onboard: (api: ApiClient, request: OnboardTenantRequest, idempotencyKey: string) => api.request<OnboardTenantResponse>("/api/platform/onboarding", { method: "POST", headers: { "Idempotency-Key": idempotencyKey }, body: JSON.stringify(request) }), leads: (api: ApiClient, query = "") => api.request<DemoLeadPage>(`/api/platform/leads${query ? `?${query}` : ""}`), leadSummary: (api: ApiClient) => api.request<DemoLeadSummary>("/api/platform/leads/summary"), lead: (api: ApiClient, id: string) => api.request<DemoLeadDetail>(`/api/platform/leads/${id}`), leadStatus: (api: ApiClient, id: string, status: string) => api.request<void>(`/api/platform/leads/${id}/status`, { method: "POST", body: JSON.stringify({ status }) }), leadAssign: (api: ApiClient, id: string, userId: string | null) => api.request<void>(`/api/platform/leads/${id}/assignment`, { method: "POST", body: JSON.stringify({ userId }) }), leadNote: (api: ApiClient, id: string, note: string) => api.request<void>(`/api/platform/leads/${id}/notes`, { method: "POST", body: JSON.stringify({ note }) }) };
