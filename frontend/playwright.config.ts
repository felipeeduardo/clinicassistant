import { defineConfig } from "@playwright/test";
export default defineConfig({ testDir: "./e2e", timeout: 30_000, reporter: "list", use: { baseURL: process.env.E2E_BASE_URL ?? "http://localhost:3000", trace: "retain-on-failure", screenshot: "only-on-failure" }, projects: [{ name: "chromium", use: { browserName: "chromium" } }], webServer: process.env.CI || process.env.E2E_SKIP === "true" ? undefined : { command: "npm run dev", port: 3000, reuseExistingServer: true } });
