import Link from "next/link";
import { Icon } from "./icon";

const labels: Record<string, string> = { dashboard: "Dashboard", conversations: "Conversas", catalog: "Catálogo", clinics: "Clínica", units: "Unidades", patients: "Pacientes", professionals: "Profissionais", specialties: "Especialidades", appointments: "Agenda", integrations: "Integrações", whatsapp: "WhatsApp", audit: "Auditoria", platform: "Plataforma", onboarding: "Onboarding" };
export function PageContainer({ children }: { children: React.ReactNode }) { return <main className="mx-auto w-full max-w-content p-4 sm:p-6 lg:p-8">{children}</main>; }
export function Breadcrumbs({ pathname }: { pathname: string }) { const items = pathname.split("/").filter(Boolean); if (!items.length) return null; return <nav className="mb-3 flex items-center gap-1 text-sm text-slate-500" aria-label="Navegação estrutural"><Link className="hover:text-slate-900" href="/dashboard">Início</Link>{items.map((item, index) => <span className="flex items-center gap-1" key={`${item}-${index}`}><Icon className="size-3.5" name="chevronRight" /><span className={index === items.length - 1 ? "font-medium text-slate-700" : ""}>{labels[item] ?? item}</span></span>)}</nav>; }
export function PageHeader({ title, description, pathname, actions }: { title: string; description?: string; pathname?: string; actions?: React.ReactNode }) { return <header className="flex flex-col gap-4 border-b border-slate-200 pb-5 sm:flex-row sm:items-end sm:justify-between"><div>{pathname && <Breadcrumbs pathname={pathname} />}<h1 className="text-2xl font-semibold tracking-tight text-slate-950 sm:text-3xl">{title}</h1>{description && <p className="mt-1 max-w-2xl text-sm text-slate-600 sm:text-base">{description}</p>}</div>{actions && <div className="flex shrink-0 flex-wrap gap-2">{actions}</div>}</header>; }
