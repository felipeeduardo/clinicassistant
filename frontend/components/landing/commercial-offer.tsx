import Link from "next/link";
import { commercialConfig } from "@/lib/commercial/config";

export function CommercialOffer() {
  const pilot = commercialConfig.mode === "pilot";
  const approvedPricing = commercialConfig.mode === "publicPricing" && commercialConfig.showPricing && commercialConfig.monthlyInvestment !== undefined;
  return <section className="bg-slate-50 text-slate-950" id="investimento"><div className="mx-auto max-w-content px-5 py-20 lg:px-8"><p className="text-sm font-semibold uppercase tracking-[.18em] text-brand-600">Investimento</p><h2 className="mt-3 max-w-2xl text-3xl font-semibold tracking-tight sm:text-4xl">{pilot ? "Piloto para clínicas parceiras." : "Uma proposta comercial transparente."}</h2><p className="mt-4 max-w-2xl text-lg leading-8 text-slate-600">{approvedPricing ? "Um investimento previsível para organizar uma das principais portas de entrada da sua clínica." : "Entenda o cenário da sua clínica e receba uma proposta adequada à operação, sem preços genéricos."}</p><div className="mt-8 max-w-2xl rounded-panel border border-slate-200 bg-white p-6 shadow-panel"><h3 className="text-xl font-semibold">{commercialConfig.badge ?? "Demonstração orientada"}</h3>{approvedPricing ? <p className="mt-3 text-3xl font-semibold">{new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL" }).format(commercialConfig.monthlyInvestment!)}<span className="ml-1 text-sm font-normal text-slate-500">/mês</span></p> : <p className="mt-3 text-sm leading-6 text-slate-600">Inclui descoberta da operação, validação do fluxo WhatsApp → agenda e definição dos próximos passos. Custos variáveis de provedores são avaliados separadamente.</p>}<Link className="mt-6 inline-flex rounded-control bg-brand-600 px-5 py-3 font-semibold text-white hover:bg-brand-700 focus:outline-none focus:ring-2 focus:ring-brand-500" href={`mailto:demo@clinicassistant.local?subject=${encodeURIComponent(pilot ? "Participar do piloto" : "Solicitar proposta")}`}>{pilot ? "Quero participar do piloto" : "Solicitar proposta"}</Link></div></div></section>;
}
