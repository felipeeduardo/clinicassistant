# Templates

Um template pertence ao tenant e à integração, usa `ContentSid`, precisa estar `Approved` e ter variáveis compatíveis com `ParametersSchema`. Texto livre é permitido apenas até 24 horas após a última mensagem recebida; após isso, a aplicação falha de forma segura quando não há template aprovado.
