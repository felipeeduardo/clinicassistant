# Smoke local

Execute `./scripts/local/start-local.sh` e depois `./scripts/local/validate.sh`. A validação verifica API e frontend sem disparar mensagem WhatsApp.

Para validar o fluxo completo Fake WhatsApp → Outbox → Worker → gateway, siga o [roteiro detalhado do smoke Fake WhatsApp](fake-whatsapp-smoke.md).
